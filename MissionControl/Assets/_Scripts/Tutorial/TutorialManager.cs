using UnityEngine;

// Responsible for displaying hints and in-game clues, and would most likely need to either hook into or communicate with other UI scripts
public class TutorialManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
